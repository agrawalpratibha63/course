import { FormEvent,useState } from "react"; import { X } from "lucide-react"; import { useApp } from "@/context/AppContext";
export default function AuthModal({onClose}:{onClose:()=>void}){const{login}=useApp();const[name,setName]=useState(""),[email,setEmail]=useState(""),[error,setError]=useState("");function submit(e:FormEvent){e.preventDefault();if(name.trim().length<2||!/^\S+@\S+\.\S+$/.test(email)){setError("Please enter a valid name and email address.");return}login(name,email);onClose()}return <div className="fixed inset-0 z-50 grid place-items-center bg-slate-950/60 p-4" role="dialog" aria-modal="true"><form onSubmit={submit} className="relative w-full max-w-md rounded-2xl bg-white p-7 shadow-2xl"><button type="button" onClick={onClose} className="absolute right-5 top-5 rounded-full p-2 hover:bg-slate-100" aria-label="Close"><X size={20}/></button><p className="text-sm font-bold uppercase tracking-wider text-blue-700">Welcome to CODES</p><h2 className="mt-2 text-3xl font-bold">Create your learner profile</h2><p className="mt-2 text-sm text-slate-600">Demo data is stored in this browser. No password is required.</p><label className="mt-6 block text-sm font-semibold">Full name</label><input value={name} onChange={e=>setName(e.target.value)} className="mt-2 w-full rounded-lg border px-4 py-3" placeholder="Pratibha Agrawal"/><label className="mt-4 block text-sm font-semibold">Email</label><input type="email" value={email} onChange={e=>setEmail(e.target.value)} className="mt-2 w-full rounded-lg border px-4 py-3" placeholder="you@example.com"/>{error&&<p className="mt-3 text-sm text-red-600">{error}</p>}<button className="mt-6 w-full rounded-lg bg-blue-700 px-5 py-3 font-bold text-white">Continue learning</button></form></div>}
