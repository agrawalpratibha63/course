import { CheckCircle2, X } from "lucide-react";
const COLORS=["#2563eb","#f59e0b","#10b981","#ec4899","#8b5cf6"];
const PIECES=Array.from({length:42},(_,i)=>({left:`${(i*37)%100}%`,delay:`${(i%9)*.08}s`,duration:`${3+(i%5)*.22}s`,color:COLORS[i%COLORS.length]}));
export default function CompletionCelebration({courseTitle,onClose}:{courseTitle:string;onClose:()=>void}){return <div className="fixed inset-0 z-[70] grid place-items-center overflow-hidden bg-slate-950/45 p-4" role="dialog" aria-modal="true" aria-live="assertive"><div className="pointer-events-none absolute inset-0" aria-hidden="true">{PIECES.map((p,i)=><span key={i} className="confetti-piece" style={{left:p.left,animationDelay:p.delay,animationDuration:p.duration,backgroundColor:p.color}}/>)}</div><div className="relative z-10 w-full max-w-md rounded-2xl bg-white p-7 text-center shadow-2xl"><button onClick={onClose} aria-label="Close celebration" className="absolute right-4 top-4 rounded-full p-2 hover:bg-slate-100"><X size={19}/></button><CheckCircle2 className="mx-auto text-emerald-500" size={56}/><p className="mt-4 text-xs font-bold tracking-widest text-blue-700">COURSE COMPLETED</p><h2 className="mt-2 text-2xl font-black">Great Job! You&apos;ve completed this course!</h2><p className="mt-3 text-sm text-slate-600">You completed <strong>{courseTitle}</strong>. Keep going—your next skill is waiting.</p><button onClick={onClose} className="mt-6 rounded-lg bg-blue-700 px-6 py-2.5 font-bold text-white">Continue learning</button></div></div>}
