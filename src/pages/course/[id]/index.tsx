import Head from "next/head"; import Link from "next/link"; import { useRouter } from "next/router"; import { useState } from "react"; import { ArrowLeft,Bookmark,CheckCircle2,Clock,PlayCircle,Star } from "lucide-react"; import { courses } from "@/Components/data/constant"; import { useApp } from "@/context/AppContext"; import AuthModal from "@/Components/AuthModal";
function embed(id:string){const match=id.match(/(?:youtu\.be\/|v=)?([\w-]{11})/);return match?`https://www.youtube-nocookie.com/embed/${match[1]}`:`https://www.youtube-nocookie.com/embed/${id.split("?")[0]}`}
export default function CoursePage(){const router=useRouter();const id=String(router.query.id||"");const course=courses.find(c=>c.id===id);const{user,enrolled,progress,enroll,toggleModule,wishlist,toggleWishlist}=useApp();const[auth,setAuth]=useState(false),[active,setActive]=useState(0);if(!course)return <main className="grid min-h-[60vh] place-items-center"><div className="text-center"><h1 className="text-3xl font-bold">Course not found</h1><Link href="/catalog" className="mt-4 inline-block text-blue-700">Browse all courses</Link></div></main>;const joined=enrolled.includes(id),done=progress[id]||[],percent=Math.round(done.length/course.modules.length*100);function join(){if(!user){setAuth(true);return}enroll(id)}return <><Head><title>{course.title} | CODES</title></Head><main><section className="bg-slate-950 text-white"><div className="mx-auto grid max-w-7xl gap-7 px-4 py-10 lg:grid-cols-[1fr_360px]"><div><Link href="/catalog" className="flex items-center gap-2 text-sm text-blue-300"><ArrowLeft size={16}/>All courses</Link><p className="mt-8 font-bold text-blue-300">{course.provider} · {course.type}</p><h1 className="mt-3 text-2xl font-black leading-tight md:text-4xl">{course.title}</h1><p className="mt-5 max-w-3xl text-lg text-slate-300">{course.description}</p><div className="mt-6 flex flex-wrap gap-5 text-sm"><span className="flex gap-1"><Star className="fill-amber-400 text-amber-400" size={18}/>{course.rating}</span><span>{course.students}</span><span>{course.level}</span><span className="flex gap-1"><Clock size={18}/>{course.timeline}</span></div></div><aside className="rounded-2xl bg-white p-6 text-slate-900 shadow-xl"><img src={course.image} alt="" className="h-44 w-full rounded-xl object-cover"/><p className="mt-5 text-2xl font-black">Start learning today</p><p className="mt-2 text-sm text-slate-600">Full access to every module and progress tracking.</p><button onClick={join} className="mt-5 w-full rounded-lg bg-blue-700 py-3 font-bold text-white">{joined?`Continue · ${percent}% complete`:"Enroll for free"}</button><button onClick={()=>toggleWishlist(id)} className="mt-3 flex w-full items-center justify-center gap-2 rounded-lg border py-3 font-bold"><Bookmark size={18} className={wishlist.includes(id)?"fill-blue-700 text-blue-700":""}/>{wishlist.includes(id)?"Saved":"Save for later"}</button></aside></div></section><section className="mx-auto grid max-w-7xl gap-7 px-4 py-10 lg:grid-cols-[1fr_360px]"><div><h2 className="text-2xl font-black">What you&apos;ll learn</h2><div className="mt-5 grid gap-3 sm:grid-cols-2">{course.skills.map(s=><div key={s} className="flex gap-2 rounded-lg bg-blue-50 p-3"><CheckCircle2 className="text-blue-700" size={20}/><span>{s}</span></div>)}</div><h2 className="mt-9 text-2xl font-black">Course content</h2><div className="mt-5 space-y-3">{course.modules.map((m,i)=><button key={m.title} onClick={()=>setActive(i)} className={`flex w-full items-center gap-4 rounded-xl border p-4 text-left ${active===i?"border-blue-600 bg-blue-50":""}`}><span className="grid h-10 w-10 place-items-center rounded-full bg-white"><PlayCircle className="text-blue-700"/></span><span className="flex-1"><b>{i+1}. {m.title}</b><small className="mt-1 block text-slate-600">{m.description} · {m.hours} hours</small></span>{done.includes(i)&&<CheckCircle2 className="text-emerald-600"/>}</button>)}</div></div><aside><div className="sticky top-24 rounded-2xl border bg-white p-5 shadow-sm"><h3 className="text-xl font-bold">Learning player</h3>{joined?<><div className="mt-4 aspect-video overflow-hidden rounded-lg bg-black"><iframe className="h-full w-full" src={embed(course.modules[active].videoId)} title={course.modules[active].title} allowFullScreen/></div><h4 className="mt-4 font-bold">{course.modules[active].title}</h4><button onClick={()=>toggleModule(id,active)} className={`mt-4 w-full rounded-lg py-3 font-bold ${done.includes(active)?"bg-emerald-50 text-emerald-700":"bg-blue-700 text-white"}`}>{done.includes(active)?"Completed — mark incomplete":"Mark module complete"}</button><div className="mt-5 h-2 overflow-hidden rounded-full bg-slate-200"><div className="h-full bg-emerald-500" style={{width:`${percent}%`}}/></div><p className="mt-2 text-sm font-semibold">{percent}% complete</p></>:<><p className="mt-3 text-sm text-slate-600">Enroll to unlock lessons and progress tracking.</p><button onClick={join} className="mt-4 w-full rounded-lg bg-blue-700 py-3 font-bold text-white">Enroll now</button></>}</div></aside></section></main>{auth&&<AuthModal onClose={()=>setAuth(false)}/>}</>}
